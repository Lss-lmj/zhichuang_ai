# Data Directory

本目录用于本地开发和 Demo 运行时数据。

建议结构：

```text
data/
  local/
    app.db
    chroma/
    uploads/
  seeds/
    demo-courses/
    demo-assignments/
```

`data/local/`、数据库文件、上传文件和向量库运行数据不进入 Git。
